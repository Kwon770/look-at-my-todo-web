import React from "react";
import styled from "styled-components";

const Rank = () => {
  return (
    <Holder>
      <h1>RANK</h1>
    </Holder>
  );
};

const Holder = styled.div``;

export default Rank;
