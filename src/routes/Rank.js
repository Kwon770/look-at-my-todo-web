import React from "react";
import styled from "styled-components";

const Rank = () => {
  return (
    <Container>
      <h1>RANK</h1>
    </Container>
  );
};

const Container = styled.div``;

export default Rank;
